# Frontend (React) - Create React App style

This frontend is a plain React app (CRA-compatible). Set `REACT_APP_API_BASE` to your backend URL (e.g. http://localhost:4000) in .env or in hosting environment variables.

Run locally:

```
cd frontend
npm install
npm start
```
